const MESSAGING_ENDPOINT = {
    BASE: 'messaging',
    CONVERSATIONS: 'conversations',
    CONVERSATION_MESSAGES: 'conversations/:conversationId/messages',
    CONVERSATION_PARTICIPANT_STATES:
        'conversations/:conversationId/participant-states',
};

export default MESSAGING_ENDPOINT;
