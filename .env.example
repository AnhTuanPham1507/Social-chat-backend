# Environment configuration has been moved to the config/ directory.
# See config/.env.example for details.
#
# Files needed:
#   config/.env.common  — shared across all services
#   config/.env.auth    — auth service specific
#   config/.env.user    — user service specific
#   config/.env.asset   — asset service specific
#   config/.env.feed    — feed service specific
